---
"@ledgerhq/hw-transport-mocker": minor
---

Move the package from the ledger-live monorepo to ts-libs. Declares its `events` dependency explicitly instead of relying on `@ledgerhq/hw-transport` to hoist it.
